{
  "name": "eleventy-deep-pagination",
  "version": "1.0.0",
  "description": "Testing multilevel pagination hack in Eleventy. Following an idea from Tobias.",
  "browser": "index.html",
  "scripts": {
    "start": "npx @11ty/eleventy --serve",
    "code": "code && npx @11ty/eleventy --serve",
    "clean": "if [ -d _site ]; then trash docs; fi && npx @11ty/eleventy --serve",
    "debug": "DEBUG=Eleventy* npx @11ty/eleventy",
    "prod": "npx @11ty/eleventy"
  },
  "author": "Mathieu Huot",
  "license": "Apache-2.0",
  "repository": {
    "type": "git",
    "url": "https://github.com/solution-loisir/eleventy-deep-pagination"
  },
  "devDependencies": {
    "@11ty/eleventy": "^0.11.1",
    "fibers": "^5.0.0",
    "sass": "^1.26.3"
  }
}
